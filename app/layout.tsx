import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "MediGuide by Hemu - Trusted Medical Information Platform",
  description:
    "Comprehensive medical information platform with symptom checker, disease database, and prevention guides. Get reliable medical guidance at your fingertips.",
  generator: "v0.app",
  keywords: "medical information, symptom checker, disease database, health guide, medical conditions, prevention",
  viewport: "width=device-width, initial-scale=1",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
